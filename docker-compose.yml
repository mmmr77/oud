services:
  oud:
    image: oud:1.0.0
    container_name: "oud"
    restart: always
    networks:
      - oud_network
    env_file:
      - ./.env
    volumes:
      - ./my_account.session:/app/my_account.session

networks:
  oud_network:
    external: true
    name: oud_network
